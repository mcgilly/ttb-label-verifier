# Copy to .env.local and fill in. Get a key at https://console.anthropic.com
# Without a key the app runs in mock mode (deterministic sample results) so the
# UI is fully demoable; with a key it calls Claude vision for real extraction.
ANTHROPIC_API_KEY=sk-ant-...
